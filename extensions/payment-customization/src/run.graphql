query RunInput {
  cart {
    buyerIdentity {
      customer {
        id
        hasTags(tags: ["Net10days", "Net30days"]) {
          tag
        }
      }
    }
    cost {
      totalAmount {
        amount
      }
    }
  }
  paymentMethods {
    id
    name
  }
}
