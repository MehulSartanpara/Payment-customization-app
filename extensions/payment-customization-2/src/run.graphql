query RunInput {
  cart {
    buyerIdentity {
      customer {
        id
        hasAnyTag(tags: ["Net30days"])
      }
    }
    cost {
      totalAmount {
        amount
      }
    }
  }
  paymentMethods {
    id
    name
  }
}